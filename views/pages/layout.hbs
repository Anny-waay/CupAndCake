<!DOCTYPE html>
<html lang="ru">
<head>
  <title>{{title}}</title>
  <meta charset="UTF-8">
  <meta name="keywords" content="торт, пирожное, десерт, сладости">
  {{>links}}
</head>
<body>
  {{>header index=isIndex}}

  {{{body}}}
  {{>sockets}}
  {{#if isIndex}}
    {{>footer}}
  {{else}}
    {{>shortFooter}}
  {{/if}}
</body>
</html>